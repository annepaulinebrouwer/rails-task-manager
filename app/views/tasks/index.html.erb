<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <p><%= link_to task.title, task_path(task) %></p>
      <p><%= task.completed %></p>
    </li>
  <% end %>
</ul>

<p><%= link_to 'Create a new task', new_task_path %></p>
